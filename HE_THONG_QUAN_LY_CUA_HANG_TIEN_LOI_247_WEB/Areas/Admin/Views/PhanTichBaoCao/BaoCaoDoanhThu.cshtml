@using HE_THONG_QUAN_LY_CUA_HANG_TIEN_LOI_247_WEB.Models.Entities;

@{
    ViewData["title"] = "Báo Cáo Doanh Thu";
}

<main class="app-content">
    <div class="app-title">
        <div>
            <h1><i class="bi bi-graph-up"></i> Báo cáo Doanh thu</h1>
            <p>Thống kê doanh thu theo kỳ</p>
        </div>
        <ul class="app-breadcrumb breadcrumb side">
            <li class="breadcrumb-item"><i class="bi bi-house-door fs-6"></i></li>
            <li class="breadcrumb-item">Phân tích & Báo cáo</li>
            <li class="breadcrumb-item active"><a asp-controller="PhanTichBaoCao" asp-action="BaoCaoDoanhThu">Báo cáo doanh thu</a></li>
        </ul>
    </div>

    <!-- Danh sách báo cáo -->
    <div class="row">
        <div class="col-md-12">
            <div class="tile">
                <div class="tile-title-w-btn d-flex justify-content-between align-items-center">
                    <h3 class="title" style="margin-bottom: 0;">Danh Sách Báo Cáo Doanh Thu</h3>
                    <a class="btn btn-success" href="#">
                        <i class="fas fa-file-excel"></i> Xuất Excel
                    </a>
                </div>

                <div class="tile-body">
                    <div class="table-responsive">
                        <table class="table table-hover table-bordered" id="sampleTable">
                            <thead>
                                <tr>
                                    <th>STT</th>
                                    <th>Mã Báo Cáo</th>
                                    <th>Kỳ Báo Cáo</th>
                                    <th class="text-end">Tổng Doanh Thu</th>
                                </tr>
                            </thead>
                            <tbody>
                                @{
                                    if (ViewData["lstBaoCaoDoanhThu"] != null)
                                    {
                                        List<BaoCaoDoanhThu> baoCaos = ViewData["lstBaoCaoDoanhThu"] as List<BaoCaoDoanhThu>;
                                        int stt = 1;
                                        foreach (BaoCaoDoanhThu bc in baoCaos)
                                        {
                                            <tr>
                                                <td>@stt</td>
                                                <td>@bc.BaoCaoId</td>
                                                <td>@bc.KyBaoCao</td>
                                                <td class="text-end fw-bold text-success">@bc.TongDoanhThu.ToString("N0") đ</td>
                                            </tr>
                                            stt++;
                                        }
                                    }
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</main>

